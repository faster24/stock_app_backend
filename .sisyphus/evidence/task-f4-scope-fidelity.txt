Task: F4 Scope Fidelity Check (deep)
Date: 2026-03-07
Plan Reference: .sisyphus/plans/laravel-11-api-scaffold.md

Final Recommendation: FAIL (do not mark F4 complete yet)

Reason for FAIL:
1) Must-NOT-Have breach in active runtime routes: `sanctum/csrf-cookie` is still registered with `web` middleware, which is part of Sanctum's SPA/cookie flow and outside PAT-only MVP scope.
2) SQLite-first plan intent is not the active testing runtime: `.env.testing` uses MariaDB.

Must-Have Boundary Checks

[PASS] Laravel 11 scaffold exists
- Evidence command: `php artisan --version`
- Evidence output: `Laravel Framework 11.48.0`

[PASS] `/api/v1` auth endpoints are canonical and present
- Evidence command: `php artisan route:list --path=api/v1 --json`
- Evidence output includes exactly:
  - `POST api/v1/register` (middleware: `api`)
  - `POST api/v1/login` (middleware: `api`)
  - `GET|HEAD api/v1/me` (middleware: `api`, `Authenticate:sanctum`)
  - `POST api/v1/logout` (middleware: `api`, `Authenticate:sanctum`)
- File evidence: `routes/api.php` defines only those four endpoints under `Route::prefix('v1')` with auth middleware only on `me/logout`.

[PASS] Service pattern with direct DI in controllers
- File evidence: `app/Http/Controllers/Api/V1/AuthController.php` constructor injects `App\Services\Auth\AuthService` directly.
- File evidence: `app/Services/Auth/AuthService.php` contains auth business logic; controller remains thin.

[PASS] Repository pattern artifacts are absent in scoped runtime directories
- Evidence command: `grep -R "RepositoryInterface\|class .*Repository" app bootstrap config routes`
- Evidence output: no matches
- Evidence command: `if grep -R "RepositoryInterface\|class .*Repository" app bootstrap config routes; then printf "repository_artifacts=FOUND\n"; else printf "repository_artifacts=NONE\n"; fi`
- Evidence output: `repository_artifacts=NONE`

Must-NOT-Have Boundary Checks

[FAIL] No SPA cookie/stateful Sanctum flow
- Evidence command: `php artisan route:list --json`
- Evidence output includes active route:
  - `GET|HEAD sanctum/csrf-cookie` (name: `sanctum.csrf-cookie`, middleware: `web`)
- Assessment: This route is active runtime surface for Sanctum SPA cookie flow and violates strict PAT-only scope fidelity.

[PASS] No role/permission system artifacts in scoped runtime directories
- Evidence command: `if grep -R -i "\broles\?\b\|\bpermissions\?\b" app bootstrap config routes; then printf "rbac_artifacts=FOUND\n"; else printf "rbac_artifacts=NONE\n"; fi`
- Evidence output: `rbac_artifacts=NONE`

[PASS-with-note] No active password reset or email verification routes exposed
- Evidence command: `php artisan route:list --json | grep -E "password|verification|verify|forgot|reset"`
- Evidence output: no matches
- File evidence note: `config/auth.php` still contains default password reset configuration/comments, but no route/controller wiring is active for reset or verification.
- Severity: inert scaffold/default config, not active runtime behavior.

Route Exposure and Runtime Surface Notes

[PASS] Root web route `/` is not active
- File evidence: `bootstrap/app.php` does not register `routes/web.php` in `withRouting(...)`.
- Evidence command: `php artisan route:list --json | grep '"uri":"/"'`
- Evidence output: no matches

[NOTE] Inert frontend scaffold still present but inactive
- File evidence present: `routes/web.php`, `resources/views/welcome.blade.php`, `vite.config.js`, `tailwind.config.js`, `postcss.config.js`, `package.json`.
- Runtime distinction: these files remain in repo but are not currently registered into active route behavior via `bootstrap/app.php`.

[NOTE] Additional non-auth route exposure
- Evidence command: `php artisan route:list --json`
- Evidence output includes `GET|HEAD storage/{path}` and `GET|HEAD up`.
- Assessment: not directly violating auth/repository guardrails, but they are active runtime routes outside the four auth endpoints.

SQLite Scope Fidelity Note

[FAIL/Drift] Active test runtime diverges from SQLite-first plan default
- File evidence: `.env.testing` currently sets `DB_CONNECTION=mariadb`.
- File evidence: `.env.example` still defaults to sqlite (`DB_CONNECTION=sqlite`, `DB_DATABASE=database/database.sqlite`).
- Assessment: plan intent remains sqlite-first, but active local test configuration is mariaDB-based.

Overall Scope Fidelity Verdict

- Core MVP auth implementation boundaries are mostly satisfied (service DI, PAT auth endpoints under `/api/v1`, no repository artifacts, no RBAC/password-reset/email-verification runtime routes).
- Scope fidelity is not strict enough to close F4 because PAT-only boundary is currently breached by active `sanctum/csrf-cookie` route, and sqlite-first runtime drift persists in `.env.testing`.
