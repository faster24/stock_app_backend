Task: F4 Scope Fidelity Check - deep (FINAL RERUN)
Timestamp: 2026-03-07T20:35:39+07:00

Verdict: PASS
Recommendation: F4 can be checked complete.

Scope baseline (from plan):
- Must-have: Laravel 11 API scaffold, service DI/no repository pattern, Sanctum PAT auth under /api/v1, SQLite configured.
- Must-not-have: SPA cookie/stateful Sanctum flow, repository artifacts, role/permission/password-reset/email-verification scope expansion, frontend/UI runtime exposure.

Evidence commands and findings:

1) Active runtime route surface (authoritative)
Command: php artisan route:list
Output:
- POST api/v1/login
- POST api/v1/logout
- GET|HEAD api/v1/me
- POST api/v1/register
- GET|HEAD storage/{path}
- GET|HEAD up

Command: php artisan route:list --json
Key output:
- api/v1/register middleware: ["api"]
- api/v1/login middleware: ["api"]
- api/v1/me middleware: ["api","Illuminate\\Auth\\Middleware\\Authenticate:sanctum"]
- api/v1/logout middleware: ["api","Illuminate\\Auth\\Middleware\\Authenticate:sanctum"]
- sanctum/csrf-cookie route: absent

Interpretation:
- Active runtime auth surface is the four MVP /api/v1 endpoints.
- PAT-only middleware policy is correct (auth:sanctum only on me/logout).
- No active SPA cookie route is exposed.

2) Sanctum PAT-only guardrail
File: config/sanctum.php
Observed:
- 'routes' => false

Interpretation:
- Sanctum provider CSRF-cookie route auto-registration is disabled.

3) API-only bootstrap vs inert scaffold files
File: bootstrap/app.php
Observed:
- withRouting(...) registers api, commands, health only.
- No web route file registration.

Interpretation:
- Runtime is API-only.
- Residual web/frontend scaffold files are inert because web routes are not registered.

4) SQLite-first defaults and fallback behavior
Files:
- .env: DB_CONNECTION=sqlite, DB_DATABASE=database/database.sqlite
- .env.testing: DB_CONNECTION=sqlite, DB_DATABASE=database/testing.sqlite
- config/database.php: default connection computes from env('DB_CONNECTION','sqlite') and conditionally falls back to mariadb only when pdo_sqlite extension is unavailable.

Command: php -r "echo 'pdo_sqlite='.(extension_loaded('pdo_sqlite') ? 'yes' : 'no').PHP_EOL; echo 'sqlite3='.(extension_loaded('sqlite3') ? 'yes' : 'no').PHP_EOL;"
Output:
- pdo_sqlite=no
- sqlite3=no

Command: php artisan tinker --execute="echo 'env_DB_CONNECTION='.env('DB_CONNECTION').PHP_EOL; echo 'resolved_database_default='.config('database.default').PHP_EOL;"
Output:
- env_DB_CONNECTION=
- resolved_database_default=mariadb

Command: php artisan migrate:status
Output:
- migrations table present; all baseline + sanctum migrations show [Ran].

Interpretation:
- Defaults are sqlite-first in local/test env files.
- This host lacks sqlite extensions, and runtime fallback resolves to mariadb by explicit config guardrail.
- Migration/runtime checks still pass under fallback, so this is controlled compatibility behavior, not scope drift.

5) Must-not-have boundary scans (scoped)
Command (repository pattern):
- grep pattern: RepositoryInterface|class\s+\w*Repository\b|interface\s+\w*Repository\b
- scope: app/**
- result: no matches

Command (roles/permissions scope creep):
- grep pattern: \b(role|roles|permission|permissions|rbac)\b
- scope: app/**, routes/**, database/**, tests/**
- result: no matches

Command (password reset/email verification scope):
- grep pattern: forgot-password|reset-password|password_reset|verify-email|email-verification
- scope: app/**, routes/**, database/**, tests/**
- result: matches only in database/migrations/0001_01_01_000000_create_users_table.php for password_reset_tokens

Command (SPA/stateful indicators in scoped code/config):
- grep pattern: EnsureFrontendRequestsAreStateful|csrf-cookie|XSRF-TOKEN
- scope: app/**, bootstrap/**, config/**, routes/**
- result: match only in bootstrap/cache/config.php (cached config artifact), no active route exposure.

Interpretation:
- No active scope-creep implementation was found in app/routes/tests.
- password_reset_tokens is a default scaffold migration artifact (inert in runtime surface; no password-reset routes/controllers/tests).

6) Runtime behavior sanity check
Command: php artisan test --filter=Auth
Output:
- PASS Tests\Feature\Auth\AuthFailureCasesTest
- PASS Tests\Feature\Auth\AuthLifecycleTest
- Tests: 8 passed (68 assertions)

Final decision:
- PASS: strict MVP runtime scope fidelity is satisfied after route/env drift remediations.
- PASS: active runtime surface is PAT-based `/api/v1` auth + framework utility routes only, with no active Sanctum SPA cookie route.
- PASS: sqlite-first defaults are restored; mariadb is only conditional runtime fallback for hosts without sqlite extensions.

F4 completion status: COMPLETE.
