Task: F4 Scope Fidelity Check - deep (RERUN)
Timestamp: 2026-03-07

Verdict: FAIL
Recommendation: Do NOT mark F4 complete yet.

Scope baseline (from plan):
- Must-have: Laravel 11 API scaffold, service DI/no repository pattern, Sanctum PAT auth under /api/v1, SQLite configured.
- Must-not-have: SPA cookie/stateful Sanctum flow, repository artifacts, role/permission/password-reset/email-verification scope expansion, frontend/UI scope.

Evidence commands and findings:

1) Active runtime route surface
Command: php artisan route:list
Key output:
- POST api/v1/register
- POST api/v1/login
- GET|HEAD api/v1/me
- POST api/v1/logout
- GET|HEAD sanctum/csrf-cookie (name: sanctum.csrf-cookie)
- GET|HEAD storage/{path}
- GET|HEAD up

Command: php artisan route:list --json
Key output:
- api/v1/register middleware: ["api"]
- api/v1/login middleware: ["api"]
- api/v1/me middleware: ["api","Illuminate\\Auth\\Middleware\\Authenticate:sanctum"]
- api/v1/logout middleware: ["api","Illuminate\\Auth\\Middleware\\Authenticate:sanctum"]
- sanctum/csrf-cookie middleware: ["web"]

Interpretation:
- API v1 auth route policy is correct (sanctum only on me/logout).
- Strict PAT-only boundary still fails because sanctum/csrf-cookie is actively exposed with web middleware.

2) API-only bootstrap registration vs inert scaffold
File: bootstrap/app.php
Observed:
- withRouting registers only api, commands, and health.
- No web route file registration present.

File: routes/web.php
Observed:
- Contains Route::get('/') returning welcome view.

Interpretation:
- web scaffold files exist but are inert at runtime under current bootstrap wiring.

3) SQLite-first posture
File: .env.example
Observed: DB_CONNECTION=sqlite, DB_DATABASE=database/database.sqlite

File: .env.testing
Observed: DB_CONNECTION=mariadb, DB_HOST=127.0.0.1, DB_PORT=3306, DB_DATABASE=stock_db, DB_USERNAME=root, DB_PASSWORD=root

File: .env
Observed: DB_CONNECTION=mariadb, DB_HOST=127.0.0.1, DB_PORT=3306, DB_DATABASE=stock_db, DB_USERNAME=root, DB_PASSWORD=root

Command: test -f database/database.sqlite && test -f database/testing.sqlite
Output: database.sqlite=yes testing.sqlite=yes

Interpretation:
- SQLite artifacts/default template exist, but active local/testing runtime defaults are MariaDB, so strict sqlite-first fidelity is not met.

4) Must-not-have boundary scans (guardrails)
Command (repository pattern):
grep pattern: RepositoryInterface|class\s+\w*Repository\b|\binterface\s+\w*Repository\b
Scope: app/**
Result: no matches

Command (roles/permissions scope creep):
grep pattern: \b(role|roles|permission|permissions|rbac)\b
Scope: app/**,routes/**,database/**,tests/**
Result: no matches

Command (password reset/email verification scope):
grep pattern: forgot-password|reset-password|password_reset|verify-email|email-verification
Scope: app/**,routes/**,database/**,tests/**
Result: matches only in database/migrations/0001_01_01_000000_create_users_table.php for password_reset_tokens table (Laravel default scaffold artifact).

Command (SPA/stateful Sanctum indicators):
grep pattern: stateful|EnsureFrontendRequestsAreStateful|csrf-cookie|XSRF-TOKEN
Scope: app/**,bootstrap/**,config/**,routes/**
Result: matches in config/sanctum.php stateful config comments/settings.

Final decision:
- PASS: service/repository boundary fidelity, /api/v1 auth policy fidelity, API-only route registration (no active / web route).
- FAIL blockers:
  1) Active sanctum/csrf-cookie web route keeps cookie-flow surface exposed (strict PAT-only breach).
  2) Active .env/.env.testing defaults are MariaDB (sqlite-first breach).

F4 completion status: NOT COMPLETE.
