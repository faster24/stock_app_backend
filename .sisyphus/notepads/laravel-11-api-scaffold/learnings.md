# Learnings

- Repository root is non-empty (`.git`, `.sisyphus`), so `composer create-project ... .` is unsafe; scaffold into temp dir then sync into root.
- No Laravel/PHP app code exists yet; all current matches are in planning files only.
- Plan currently has 45 unchecked checkboxes in total.
- Task 1 bootstrap succeeded with `composer create-project laravel/laravel ".laravel-bootstrap-tmp" "^11.0"`, copied into root with `cp -a`, and verified by `php artisan --version` showing Laravel 11.48.0.
- Laravel 11 API routes are opt-in (`php artisan install:api`) and Sanctum PAT flow requires adding `HasApiTokens` on `User` model.
- CI/testing reliability for SQLite needs absolute `DB_DATABASE` path in `.env.testing`, explicit sqlite file creation, and config cache clear before tests.
- SQLite-first env setup uses `DB_CONNECTION=sqlite` in both `.env.example` and `.env.testing`; `.env.testing` should use an absolute `DB_DATABASE` path to avoid path-resolution drift in test runners.
- Exposing `DB_BUSY_TIMEOUT`, `DB_JOURNAL_MODE`, and `DB_SYNCHRONOUS` through `config/database.php` keeps SQLite tuning environment-driven without changing code.
- Task 3 established service-layer guardrails under app/Services with a base  abstraction, an  skeleton, and a local README that codifies concrete-service DI and explicitly bans repository-layer artifacts.
- Laravel container resolution was verified for  via bootstrap-time  check.
- Repository-pattern guardrail verification () produced no matches (exit 1 expected for no hits).
- Correction (Task 3): app/Services now contains a base Service abstraction, an AuthService skeleton, and README guardrails enforcing concrete service DI with no repository layer.
- Correction (Task 3): Laravel container resolves App\Services\Auth\AuthService successfully via app()->make().
- Correction (Task 3): repository-pattern scan across app/bootstrap/config/routes returned no matches.
- Task 4: `php artisan install:api --no-interaction` installs `laravel/sanctum`, publishes `config/sanctum.php`, publishes `routes/api.php`, and creates `create_personal_access_tokens_table` migration for PAT baseline.
- Task 4: For PAT readiness, `app/Models/User.php` must include `Laravel\Sanctum\HasApiTokens`; trait wiring was added.
- Task 4: `php artisan route:list` runs and includes `api/user` protected by `auth:sanctum` once API install wiring is in place.
- Task 4 correction: `User` model was intentionally left unchanged in this checkbox scope; only Sanctum/API package+route foundation was wired.
- Task 5: `AuthService` now owns typed auth flows (`register`, `login`, `me`, `logout`) and issues PATs with `createToken('auth_token')->plainTextToken` for both register and login paths.
- Task 5: invalid credential handling in service is deterministic via `AuthenticationException('Invalid credentials.')`, enabling consistent controller-to-HTTP mapping.
- Task 5: logout logic is scoped to current token revocation only (`currentAccessToken()->delete()`), preserving other active sessions/tokens.
- Task 5 correction: registration must hash incoming passwords explicitly in service (`Hash::make($password)`) before persistence; relying on raw assignment is a critical auth bug.
- MariaDB override for this task is environment-only: set `DB_CONNECTION=mariadb` with `DB_HOST=127.0.0.1`, `DB_PORT=3306`, `DB_DATABASE=laravel`, `DB_USERNAME=root`, and blank `DB_PASSWORD` in `.env`, `.env.example`, and `.env.testing`.
- Laravel config bootstrap now validates under MariaDB wiring (`php artisan config:clear && php artisan config:cache` both pass), isolating remaining failures to database authentication rather than driver support.
- Task 6: Added `AuthController` under `app/Http/Controllers/Api/V1` with direct `AuthService` DI and thin methods delegating register/login/me/logout logic.
- Task 6: Centralized auth request validation envelope via `App\Http\Requests\Auth\AuthFormRequest` so validation failures consistently return `message`, `data`, `errors` with 422.
- Task 7: `routes/api.php` now uses a `Route::prefix('v1')` group for auth endpoints with exactly four routes: `POST /api/v1/register`, `POST /api/v1/login`, `GET /api/v1/me`, and `POST /api/v1/logout`.
- Task 7: Sanctum protection is scoped only to `me` and `logout` via nested `Route::middleware('auth:sanctum')`, while `register` and `login` remain public.
- Task 7: Default unversioned `/api/user` route was removed to keep auth surface deterministic and avoid unversioned auth-policy drift outside `/api/v1`.
- Task 2 (SQLite reset): `.env.example` now defaults to `DB_CONNECTION=sqlite` with `DB_DATABASE=database/database.sqlite`, and `.env.testing` now defaults to `DB_CONNECTION=sqlite` with deterministic `DB_DATABASE=database/testing.sqlite`.
- Task 2 (SQLite reset): grep validation found no `mariadb`/`mysql` or `DB_HOST`/`DB_PORT`/`DB_USERNAME`/`DB_PASSWORD` defaults remaining in `.env.example` or `.env.testing`.
- Task 2 (SQLite reset): `php artisan config:clear` and `php artisan config:cache` both succeed with exit code 0 after env default updates.
- Task 3 guardrail verification (current run): `app/Services` exists with `Service` base abstraction, `Auth/AuthService` concrete implementation, and `app/Services/README.md` documenting thin-controller concrete-service DI boundaries.
- Task 3 guardrail verification (current run): repository scan command `grep -R "RepositoryInterface\|class .*Repository" app bootstrap config routes` returned no matches (exit 1 expected for clean state).
- Task 3 guardrail verification (current run): `AuthController` constructor still uses concrete service DI (`AuthService $authService`), preserving controller/service boundary.
- Task 3 guardrail verification (current run): `php artisan optimize:clear` completed successfully with exit code 0.
- Task 4 PAT wiring fix: `app/Models/User.php` now imports `Laravel\Sanctum\HasApiTokens` and includes `HasApiTokens` in the model trait list, enabling `createToken(...)`/token revocation APIs on `User`.
- Task 4 verification: `php artisan tinker --execute="echo class_exists('Laravel\\Sanctum\\Sanctum') ? 'ok' : 'fail';"`, `php artisan migrate:fresh --force`, and `php artisan route:list` all completed with exit code 0 (opcache warning remains non-blocking).
- Task 8: Added `RefreshDatabase` feature coverage under `tests/Feature/Auth/AuthLifecycleTest.php` and `tests/Feature/Auth/AuthFailureCasesTest.php` for register/login/me/logout happy paths and duplicate email, invalid credentials, and unauthenticated access failures.
- Task 8: Revoked-token lifecycle assertion in a single test process needs `$this->app['auth']->forgetGuards()` before the post-logout `/api/v1/me` request so Sanctum re-resolves auth from token storage instead of cached guard state.
- Task 8: Required verification commands `php artisan test --filter=AuthLifecycle`, `php artisan test --filter=AuthFailureCases`, and `php artisan test --filter=Auth` all run successfully after test DB override to MariaDB in `.env.testing`.
- Task 8 QA retry: `.env.testing` was restored to SQLite baseline with `DB_CONNECTION=sqlite`, `DB_DATABASE=database/testing.sqlite`, and a valid `APP_KEY` to prevent `MissingAppKeyException` during test bootstrap.
- Task 8 QA retry: auth feature tests themselves remain unchanged and still satisfy lifecycle/failure envelope assertions; failures under restored baseline are environmental (sqlite driver availability), not assertion regressions.
- Task 8 stabilization: restoring `.env.testing` to MariaDB runtime credentials (`mariadb` / `127.0.0.1:3306` / `stock_db` / `root` / `root`) with `APP_KEY` present allows plain `php artisan test --filter=Auth*` execution without command-line DB overrides.
- Task 8 stabilization: required commands `php artisan test --filter=Auth`, `php artisan test --filter=AuthLifecycle`, and `php artisan test --filter=AuthFailureCases` all complete successfully in this environment.
- Task 9: Added minimal GitHub Actions workflow at `.github/workflows/ci.yml` to run on push and pull_request with a single PHP 8.3 job.
- Task 9: CI pipeline installs Composer dependencies, prepares sqlite database file, generates APP_KEY, runs `php artisan migrate --force`, and executes `php artisan test` with sqlite env values.
- F2 review: success-path auth endpoints keep a consistent envelope (`message`, `data`, `errors`) with expected status codes (201 register, 200 login/me/logout, 401 invalid credentials, 422 validation).
- F2 review: token lifecycle is intentionally single-session revocation (`currentAccessToken()->delete()`), and lifecycle test verifies revoked-token denial after explicit guard reset in-process.
- F2 review: anti-pattern scan across scoped auth services/controllers/requests/tests/routes/CI found no `TODO|FIXME|HACK` markers or debug calls (`dd`, `dump`, `var_dump`, `die`, `exit`).
- Task F3 manual QA (2026-03-07 12:49:09Z): Live curl sweep on /api/v1/register|login|me|logout matched expected statuses (201, 422, 200, 401, 200, 401, 200, 401) using Accept: application/json; no 30x redirects or HTML error bodies observed in failure paths.
- Task F3 manual QA: Auth controller-managed responses keep message/data/errors envelope, while auth:sanctum unauthenticated responses come from framework exception handler and currently return message-only JSON ({"message":"Unauthenticated."}).

- F1 audit (2026-03-07): Tasks 1, 5, 6, 7, 8, 9 are implementation-complete by repository evidence; Task 10 remains blocked due missing full-gate command evidence (`php artisan test` and final `php artisan migrate:fresh --force`) in committed artifacts.
- F1 audit (2026-03-07): Plan-to-runtime drift is explicit: `.env` and `.env.testing` are currently MariaDB-based while plan acceptance is SQLite-first; CI workflow still enforces SQLite with `DB_CONNECTION=sqlite`.
- F4 scope fidelity (2026-03-07): Core MVP scope is present (Sanctum PAT service/controller flow, `/api/v1` routes, auth feature tests, CI), and repository-pattern artifacts are absent from `app/`, `bootstrap/`, `config/`, and `routes/`.
- F4 scope fidelity (2026-03-07): sqlite-first is partially retained (`.env.example` and CI are sqlite) but active runtime/test files (`.env`, `.env.testing`) are MariaDB-based by explicit user-driven verification choice.
- F4 scope fidelity (2026-03-07): API-only boundary is not fully enforced because default web/UI scaffold remains (`routes/web.php` welcome route, `resources/views/welcome.blade.php`, Vite/Tailwind frontend pipeline files).
- Scope remediation (2026-03-07): `bootstrap/app.php` no longer registers `routes/web.php` in `withRouting(...)`, disabling the default `/` welcome-page route while keeping API, console commands, and `/up` health wiring active.
- Scope remediation verification (2026-03-07): `php artisan route:list` no longer shows `GET|HEAD /` and still lists `/api/v1/register`, `/api/v1/login`, `/api/v1/me`, and `/api/v1/logout`.
- Auth envelope fix (2026-03-07): `bootstrap/app.php` now handles `AuthenticationException` in `withExceptions(...)` and returns the API envelope (`message`, `data`, `errors`) with HTTP 401 for `expectsJson()` requests.
- Auth envelope fix verification (2026-03-07): manual `php artisan serve` + curl checks confirmed unauthenticated `GET /api/v1/me` and `POST /api/v1/logout` now return `{"message":"Unauthenticated.","data":null,"errors":{"auth":["Authentication is required."]}}` with status 401.
- Regression verification (2026-03-07): authenticated auth flow remains unchanged in manual checks (`register` 201, `me` with bearer token 200, `logout` 200, post-logout `me` 401) and `php artisan test --filter=AuthLifecycle` passes.
- F4 scope fidelity deep check (2026-03-07): canonical auth surface remains correct under `/api/v1` with sanctum middleware only on `me` and `logout`, and repository pattern artifacts are still absent across `app/`, `bootstrap/`, `config/`, and `routes/`.
- F4 scope fidelity deep check (2026-03-07): active runtime route inventory includes `sanctum/csrf-cookie` (web middleware) from Sanctum in addition to the four MVP auth endpoints, so strict PAT-only scope is not fully satisfied despite no web `/` route registration.
- F4 scope fidelity deep check (2026-03-07): inert scaffold files (`routes/web.php`, `resources/views/welcome.blade.php`, Vite/Tailwind configs) remain in-repo but are non-runtime because `bootstrap/app.php` does not register web routes.
- Task F3 manual QA rerun (2026-03-07T13:08:32Z): live curl sweep reconfirmed full auth matrix on `/api/v1` with expected statuses and envelope shape: unauthorized `me/logout` 401, register 201, duplicate register 422, login 200, invalid login 401, authorized `me/logout` 200, post-logout `me` 401.
- Task F3 manual QA rerun: payload-key assertions passed for `message`, `data`, and `errors` across all success/failure paths, including `errors.auth`, `errors.email`, `errors.credentials`, and success-path token/user payload fields.
- F2 code quality review (2026-03-07 13:07:59Z): auth envelope contract is now consistently implemented across controller success/error responses, form-request validation failures, and unauthenticated middleware exceptions via `bootstrap/app.php` handler.
- F2 code quality review (2026-03-07 13:07:59Z): controller/service layering remains clean (`AuthController` orchestration + `AuthService` business logic), and anti-pattern scan over scoped files found no TODO/FIXME/HACK/debug markers.

- F1 compliance audit (2026-03-07 13:08:43Z): strict command rerun confirms most acceptance commands pass, including Laravel 11 version, Sanctum class resolution, v1 route listing, migrate:fresh, Auth-focused tests, and CI workflow checks.
- F1 compliance audit (2026-03-07 13:08:43Z): `php artisan test` currently fails because `tests/Feature/ExampleTest.php` still expects `/` = 200 while API-only routing now returns 404; this blocks DoD line 49 and Task 10 line 470.
- F1 compliance audit (2026-03-07 13:08:43Z): Task 5 acceptance line 277 command is path-stale (`app/Services/AuthService.php`), while actual service file is `app/Services/Auth/AuthService.php`.
- ExampleTest remediation (2026-03-07): `tests/Feature/ExampleTest.php` assertion was updated from `assertStatus(200)` to `assertStatus(404)` so the default `/` check matches API-only routing where web routes are not registered.
- ExampleTest remediation verification (2026-03-07): `php artisan test --filter=ExampleTest` and `php artisan test` both pass; environment still emits non-blocking opcache warnings and PDO MySQL constant deprecation notices.
- Task 5 remediation (2026-03-07): added compatibility shim `app/Services/AuthService.php` in namespace `App\Services` extending canonical `App\Services\Auth\AuthService`, preserving primary implementation path and behavior while making strict acceptance lint path valid.
- F4 scope fidelity rerun (2026-03-07): `php artisan route:list` now shows 7 active routes: the four `/api/v1` auth endpoints, `sanctum/csrf-cookie`, `storage/{path}`, and `/up`.
- F4 scope fidelity rerun (2026-03-07): `php artisan route:list --json` confirms policy fidelity for auth endpoints (`auth:sanctum` only on `POST api/v1/logout` and `GET|HEAD api/v1/me`; `register/login` remain public under `api` middleware).
- F4 scope fidelity rerun (2026-03-07): active routing is API-only at app bootstrap (`bootstrap/app.php` registers `api`, `commands`, and `health` only; no `web` route file registration), so `routes/web.php` and frontend scaffold files are currently inert, not runtime-exposed.
- F4 scope fidelity rerun (2026-03-07): SQLite-first posture is split by file role: `.env.example` remains sqlite (`DB_CONNECTION=sqlite`), while active local/testing runtime defaults remain MariaDB in `.env` and `.env.testing`; both sqlite database files still exist (`database/database.sqlite`, `database/testing.sqlite`).
- F4 scope fidelity rerun (2026-03-07): repository-pattern boundary remains clean on rerun (`grep` scan for `RepositoryInterface|class .*Repository|interface .*Repository` under `app/**` returned no matches).

- F1 compliance audit rerun (2026-03-07T13:20:16Z): strict acceptance/DoD command set now passes end-to-end (`php artisan test`, `php artisan migrate:fresh --force`, route checks, repository anti-pattern scans, Sanctum class check, and CI file/grep checks all exit 0).
- F1 compliance audit rerun (2026-03-07T13:20:16Z): prior blockers are resolved (`tests/Feature/ExampleTest.php` no longer breaks full-suite run; `php -l app/Services/AuthService.php` now succeeds via compatibility class at the literal acceptance path).
- F1 compliance audit rerun (2026-03-07T13:20:16Z): remaining strict gap is plan-intent drift only—`.env.testing` still uses MariaDB while plan requires SQLite-first local/test configuration.

- F1 compliance audit rerun addendum (2026-03-07T13:20:16Z): `php artisan route:list --json` still exposes `GET|HEAD sanctum/csrf-cookie` with `web` middleware; under strict reading of plan guardrail line 62, this keeps SPA-cookie Sanctum surface enabled and blocks full plan compliance.
- Sanctum route-surface fix (2026-03-07): setting `config/sanctum.php` key `'routes' => false` disables provider auto-registration of `/sanctum/csrf-cookie` while leaving PAT bearer-token auth flows unchanged.
- Sanctum route-surface verification (2026-03-07): `php artisan route:list | grep sanctum/csrf-cookie` returned no route matches, and `php artisan route:list | grep -E "api/v1/(register|login|me|logout)"` still returned exactly four auth routes.

- SQLite-first drift remediation (2026-03-07T13:31:53Z): `.env` and `.env.testing` now default to `DB_CONNECTION=sqlite` with `DB_DATABASE` set to `database/database.sqlite` and `database/testing.sqlite`, while keeping MariaDB runtime credentials available for deterministic host fallback.
- SQLite-driver fallback remediation (2026-03-07T13:31:53Z): `config/database.php` now computes default connection at bootstrap and falls back from `sqlite` to `mariadb` only when `pdo_sqlite` is unavailable; mariadb database name resolves via `DB_MARIADB_DATABASE` before `DB_DATABASE`.
- Drift verification (2026-03-07T13:31:53Z): `php artisan config:clear`, `php artisan test`, `php artisan migrate:fresh --force`, and `php artisan route:list` all completed with exit code 0 under the fallback behavior in this environment.
- F4 final rerun (2026-03-07T20:35:39+07:00): `php artisan route:list` and `php artisan route:list --json` show exactly six active routes (`/api/v1/{register,login,me,logout}`, `storage/{path}`, `/up`) with no active `sanctum/csrf-cookie` route.
- F4 final rerun (2026-03-07T20:35:39+07:00): Sanctum PAT-only boundary is runtime-enforced by `config/sanctum.php` with `'routes' => false`, and `/api/v1/me` + `/api/v1/logout` remain the only auth:sanctum-protected endpoints.
- F4 final rerun (2026-03-07T20:35:39+07:00): sqlite-first defaults are present in `.env` and `.env.testing`; host-level sqlite extension absence (`pdo_sqlite=no`, `sqlite3=no`) deterministically triggers `config/database.php` fallback to `mariadb` (`config('database.default')=mariadb`).
- F4 final rerun (2026-03-07T20:35:39+07:00): scoped guardrail scans remained clean for repository artifacts and RBAC keywords; `password_reset_tokens` appears only in Laravel default migration scaffold and is not runtime-exposed by routes/controllers/tests.

- F1 final rerun (2026-03-07T13:36:56Z): strict acceptance commands now pass end-to-end from current repository state (version, v1 route checks, migrate status/fresh, Sanctum class check, Task 5 lint path, full test suite, CI file/grep checks, and repository anti-pattern scan).
- F1 final rerun (2026-03-07T13:36:56Z): prior blockers are all resolved, including ExampleTest full-suite mismatch, literal AuthService lint-path check, sqlite-first env drift, and Sanctum csrf-cookie route exposure; final audit verdict is PASS with no stale blockers.

- Plan state sync (2026-03-07): marked Final Verification Wave F1-F4 as complete in `.sisyphus/plans/laravel-11-api-scaffold.md` based on verified evidence artifacts.
